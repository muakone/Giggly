# Copy this file to .env and fill values

# Supabase (Public keys are safe to expose to client builds; do not commit real keys)
# Get these from your Supabase project -> Settings -> API
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
EXPO_PRIVATE_SUPABASE_SERVICE_KEY=

# Example payment provider keys (if used)
EXPO_PUBLIC_PAYSTACK_PUBLIC_KEY=pk_test_xxx
EXPO_PUBLIC_PAYSTACK_SECRET_KEY=sk_test_xxx

# Quick usage (bash / WSL):
# export EXPO_PUBLIC_SUPABASE_URL="https://your-project.supabase.co"
# export EXPO_PUBLIC_SUPABASE_ANON_KEY="your-anon-key"
# npx expo start -c

# Notes:
# - Supabase supports email/password auth by default. To enable phone sign-in
#   (SMS OTP) configure an SMS provider in Supabase Auth settings (e.g. Twilio).
# - If you configure Supabase, install the SDK and restart Metro. The app's
#   `lib/supabase.ts` wrapper will dynamically enable auth when keys are present.
